# Mobile navigation

The first native flow mirrors the SHABDIZ mobile design: authentication first, then the main Home/Explore experience. Screen-level navigation will be expanded as each existing web capability is mapped to native UI.
